{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
      {
        "customType": "regex",
        "fileMatch": [
          "(^|/)Dockerfile$"
        ],
        "matchStrings": [
          "FROM (?<depName>.*):.*-jre-(?<currentValue>.*)"
        ],
        "extractVersionTemplate": "^.*-jre-(?<version>.*)$",
        "versioning": null,
        "versioningTemplate": "ubuntu",
        "datasourceTemplate": "docker"
      }
    ]
}
